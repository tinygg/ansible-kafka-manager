#!/usr/bin/env bash
echo "stopping kafka-manager..."

pid_from_file=$(echo `cat {{ kafka_manager_dir }}/RUNNING_PID`)
echo "PID is "$pid_from_file" (from RUNNING_PID)."
kill -9 $pid_from_file

#kafka_manager_pid=$(echo `ps aux | grep kafka-manager | grep -v grep | awk '{ print $2 }'`)
#pid=$kafka_manager_pid
#echo "Shutting down Kafka Manager"
#kill -9 $pid

rm -rf {{ kafka_manager_dir }}/RUNNING_PID

exit 0
